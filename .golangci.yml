run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
    - gocyclo
    - gofmt
    - goimports
    - misspell
    - unconvert
    - goconst
    - gocritic
    - revive

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
